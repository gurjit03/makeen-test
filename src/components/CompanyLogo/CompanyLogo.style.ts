import styled from 'styled-components'
import { colors, fontSizes } from '../../config/jss'

export const Logo = styled.h3`
	font-size: ${fontSizes.large}px;
	text-transform: uppercase;
	color: ${colors.white};
`
